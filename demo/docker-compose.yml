# tag::edge[]
version: '2.4'

services:
  edge:
    image: traefik:1.7.7
    command: 
      - "--docker" # Tells Traefik to listen to docker
    ports:
      - "80:80"     # The HTTP port
      - "443:443"   # The HTTPS port
    volumes:
      # To communicate with the Docker Engine
      - /var/run/docker.sock:/var/run/docker.sock
  # end::edge[]
  # tag::web[]
  web:
    build: ./web/
    labels:
      - "traefik.frontend.rule=Host:traefik-demo.containous.cloud"
  # end::web[]
  # tag::jenkins[]
  jenkins:
    image: jenkins/jenkins:2.150.2-alpine
    expose:
      - 8080
      - 50000
    environment:
      - JENKINS_OPTS=--prefix=/jenkins
    labels:
      - "traefik.port=8080"
      - "traefik.frontend.rule=Host:traefik-demo.containous.cloud;PathPrefix:/jenkins"
  # end::jenkins[]
  # tag::gitserver[]
  gitserver:
    image: gitea/gitea:latest
    expose:
      - "3000"
      - "22"
    environment:
      - ROOT_URL=gitserver
    labels:
      - "traefik.port=3000"
      - "traefik.frontend.rule=Host:traefik-demo.containous.cloud;PathPrefixStrip:/gitserver"
  # end::gitserver[]
  # tag::ttyd[]
  ttyd:
    image: tsl0922/ttyd
    labels:
      - "traefik.frontend.rule=Host:traefik-demo.containous.cloud;PathPrefixStrip:/ttyd"
  # end::ttyd[]
