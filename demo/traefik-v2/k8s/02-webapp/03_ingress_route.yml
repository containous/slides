---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: webapp
  namespace: webapp
  labels:
    app: webapp
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`webapp.localhost`)
    kind: Rule
    services:
    - name: webapp
      port: 80
