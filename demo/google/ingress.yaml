---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: frontend
spec:
  entryPoints:
    - http
  routes:
    - match: Host(`frontend.docker.localhost`)
      kind: Rule
      services:
        - name: frontend
          port: 80
  tls:
    certResolver: default

